echo -e 打印命令
alias 别名
echo -e[识别转移符] 内容
alias 别名 
命令 > 文件名  正确命令覆盖输出到文件
命令 >> 文件名  正确命令追加输出到文件
命令 2> 文件名  错误命令覆盖输出到文件
命令 2>>文件名  错误命令追加输出到文件 
命令 >>文件 2>&1  追加的方式, 错误正确命令输出到同一文件
命令 &>>文件名  追加的方式, 错误正确命令输出到同一文件 
命令 >> 文件名1 2>>文件名2 正确追加到文件1  错误追加到文件2
命令 &> /dev/null 垃圾袋 

命令连接符 
 分号 ;  && 逻辑与  || 逻辑或 
 
 管道符
 
 netstat -anl
 wc 统计 
 Linux 符号 
  '' 所有符号失去意义
  "" 中 $  ` 反斜杠 \ 除外 
  `` 反引号
  bb = $(命令) 命令的结果赋值 给变量 bb
  $ 调用变量的值
	
	
 vim 
 u 回退
 dd 删除一行
 yy 复制光标所在行
 o 在光标下一行插入  并变成输入模式
 p/P 在光标 下/上行粘贴
 wq 保存
 q! 不保存
 vim [+数字] 文件 
 vim +/xxxx 文件 光标落在 xxxx 第一次出现的位置
 
变量叠加 
${变量名}值
"$变量名"值

变量查看  set 
set -u 
unset 变量名 删除变量
 
pstree 查看进程树

声明环境变量
export 变量名=变量值
env 查看环境变量 
变量声明类型 decalre 
read 等待输入

 cut 文件内容分割 -d 指定分割表达式  -f 指定分割后显示第几个 -c 2-3截取
 
 awk 行处理器  可以做字符串分割

流程控制 :
 if [] : then 
    do
 fi
 
 
 格式二
 if []
	then 
	 do
 fi		


if else 语法
if [  ] 
   then
     执行代码

   else
      执行代码

 fi
 
 
 sed :
